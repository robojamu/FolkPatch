name: 每日自动同步检测

on:
  schedule:
    - cron: '0 18 * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: 检查对方更新
        id: check
        run: |
          echo "=== 开始检测 ==="
          
          # 添加远程仓库
          git remote add matsuzaka https://github.com/matsuzaka-yuki/FolkPatch.git 2>/dev/null || true
          git fetch matsuzaka
          
          # 获取提交哈希
          LOCAL=$(git rev-parse HEAD)
          REMOTE=$(git rev-parse matsuzaka/main)
          
          echo "本地提交完整哈希: $LOCAL"
          echo "远程提交完整哈希: $REMOTE"
          echo ""
          echo "本地提交(简短): ${LOCAL:0:8}"
          echo "远程提交(简短): ${REMOTE:0:8}"
          
          # 详细比较
          if [ "$LOCAL" = "$REMOTE" ]; then
            echo "✅ 状态：无更新（代码相同）"
            echo "has_updates=false" >> $GITHUB_OUTPUT
          else
            echo "🔄 状态：发现更新！"
            echo "has_updates=true" >> $GITHUB_OUTPUT
            echo "new_commit=$REMOTE" >> $GITHUB_OUTPUT
            
            # 显示新提交内容
            echo ""
            echo "📝 新提交内容："
            git log --oneline HEAD..matsuzaka/main
          fi
          
          echo "=== 检测结束 ==="
      
      - name: 创建Pull Request
        if: steps.check.outputs.has_updates == 'true'
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: "sync-${{ github.run_id }}"
          title: "🔄 自动同步更新"
          body: |
            检测到 [matsuzaka-yuki/FolkPatch](https://github.com/matsuzaka-yuki/FolkPatch) 有新的提交。
            
            请审查后手动合并。
          labels: "auto-sync"
