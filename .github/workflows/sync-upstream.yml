name: Daily Sync Upstream (Auto Create PR)

on:
  schedule:
    - cron: '0 18 * * *'  # UTC 18:00 = 北京时间凌晨 2:00
  workflow_dispatch:

jobs:
  sync-upstream:
    runs-on: ubuntu-latest
    steps:
      - name: Sync from upstream
        uses: tgymnich/fork-sync@v2.0.10   # 这里改成这个
        with:
          owner: matsuzaka-yuki
          repo: FolkPatch
          base: main
          head: main
          token: ${{ secrets.PAT || github.token }}
          auto_approve: false
          auto_merge: false
          pr_title: 'Sync upstream: Merge latest changes from matsuzaka-yuki/FolkPatch'
          pr_message: |
            🤖 自动检测到上游有新更新，已创建同步 PR。

            请手动审阅变更后合并。

            如果有冲突，请手动解决后合并。

            上游仓库：https://github.com/matsuzaka-yuki/FolkPatch
